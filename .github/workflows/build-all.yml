name: Build All Platforms

on:
  workflow_dispatch:

# Android APK is handled by "Build Android APK" workflow only.
# This file is manual-only so it does not fight the APK job.

permissions:
  contents: write

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use workflow Build Android APK for the phone APK."
